#include "file.h"

/**
 * main - the main function.
 * @ac: the counter.
 * @av: array vector.
 * @ev: environment variable.
 * Return: returns 0.
 */

int main(int ac, char **av, char **ev)
{
if (ac == 1)
prompt(av, ev);
return (0);
}
