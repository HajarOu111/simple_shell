#include "file.h"

/**
 * sum - a function that adds two integers.
 * @a: the first integer.
 * @b: the second integer.
 * Return: Returns the result of the addition.
 */

int sum(int a, int b)
{
return (a + b);
}
